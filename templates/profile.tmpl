<div class="profile">
    ${if(it.img)}
        <img class="profile__img" src="img/bios/${it.img}" alt="Profile image of ${it.name}">
    ${endif}
    <div class="profile__description">
        ${it.html}
    </div>
</div>